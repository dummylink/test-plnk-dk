===============================================================================
 POWERLINK CN Development Kit (CNDK) - TODOs and Questions
===============================================================================

-------------------------------------------------------------------------------
TODOs
-------------------------------------------------------------------------------
- prepare Digital I/O application on muliprocessor NIOS to be stored in flash
  (at the moment only the PCP application could be stored on flash, how could
  we store elf files for multiple NIOS processor on EPCS?)
 - cleanup CnApiDebug.h (do we need it?, AP application and CnApi should be
  independant of openPOWERLINK, therfore we couldn't use openPOWERLINK Debug.h,
  On the other side, the PCP application has to use Debug.h because it includes
  the openPOWERLINK files) 

doc
---
- generate doxygen documentation
- create user manual

fpga
----


libCnApi
--------
- implement endian conversion if necessary (to be runned on little and big endian processors)
- cleanup for doxygen
- prepare doxygen for internal and API reference manual documentation
- implement CnApi_doReadObjReq in cnApiAsync.c
- implement a way for the user to register callback functions (e.g. to write/read objects asynchronously in operating mode)
- move definitions only used by libCnApi itself from cnApi.h to cnApiIntern.h
 
-------------------------------------------------------------------------------
Questions
-------------------------------------------------------------------------------
Q: How do we configure the framework (path of openPOWERLINK,...)
  -> shellscript to be included in create-this-app for altera
  -> could it be done the same way for xilinx?
  
Q: powerlink application (PCP) must be compiled for specific application because
  the object dictionary must be compiled in. Could we configure the application
  for a specific object dictionary or do we need to setup separate applications
  for each object dictionary?  